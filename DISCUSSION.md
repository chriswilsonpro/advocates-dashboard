Wishlist with more time:

I would have liked to tie the database schema more closely to the Typescript definitions.
There is still a lot of potential improvements cleaning up the formatting and styling of the Advocate Table.
For extremely large datasets, I would expand the useAdvocates hook to load a limited subset of the data and add to results set as searches are performed.
A visual indicator that additional data is being requested from the database is also a priority.
